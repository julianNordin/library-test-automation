# Normalise to LF in the repository. The tree mixes C#, TypeScript, YAML and
# PowerShell; a single line ending keeps diffs honest across all of them.
* text=auto eol=lf

# Never touch these.
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
*.pdf binary
*.zip binary
